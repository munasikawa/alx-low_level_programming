#include "main.h"

/**
 *  _islower - checks if character is lowercased
 *  @c: contains the value to be compared.
 *  Return: Always 1 or 0 depends
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
